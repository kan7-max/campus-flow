# 2026-05-07 AIプレビュー候補UI調整メモ

## 今回の目的

`/ai` の複数課題候補プレビューを、`/inbox` と同じ感覚で候補ごとに確認しやすくする。

## 変更したファイル

- `src/components/ai/ai-chat-panel.tsx`
- `docs/update-memos/2026-05-07-ai-preview-candidate-ui.md`

## 変更内容

- `/ai` の抽出プレビューを、候補ごとの折り畳み表示に変更
- プレビュー上部に以下の状態表示を追加
  - 候補数
  - 選択中件数
  - 保存済み件数
- 候補ごとに以下を一覧で分かるように調整
  - 課題候補番号
  - 課題名
  - 授業名
  - 締切
  - 未保存 / 保存済み
  - 保存対象
  - 課題タイプ
  - 信頼度
  - 要確認
- 保存前の案内文を追加
  - `保存前に締切と授業だけ確認してください。`
- 保存対象チェックの文言を `保存対象にする` に変更
- 既存の抽出・編集・選択保存ロジックは維持

## 確認結果

- `/ai` で複数候補抽出プレビューが表示されることを確認
- 候補3件、選択中2件の状態表示を確認
- 候補ごとの折り畳み表示を確認
- 保存対象チェック表示を確認
- ブラウザconsole error 0件を確認
- build後に `.next` を削除し、demo mode dev serverを再起動済み

## 実行コマンド

- `npm run lint` OK
- `npm run typecheck` OK
- `npm run qa:mvp` OK
  - `/today` は既存優先枠の都合で、保存QA課題が上位表示されないWARNあり
- `npm run qa:inbox` OK
- `npm run check` OK
- `TASKFLOW_DEMO_MODE=1 npm run build -- --no-lint` OK

## 残っている課題

- `/ai` の保存後に候補ごとの保存済み表示は出るが、Inboxのような候補単位の個別保存ボタンではなく、選択一括保存のまま
- `/today` は既存課題の優先順位によって、新規保存課題が必ず上位3件に出るわけではない

## 次に優先すべき作業

1. `/ai` 保存後の候補別保存済み状態をブラウザで追加確認
2. `/today` の優先度スコアを、MVP導線で新規・直近締切が見えやすいよう微調整
3. Home / Today の低優先パネル折り畳み状態をスマホ幅で再確認
