# 2026-05-07 追加作業メモ（AIプレビュー作成ボタン押下性改善）

## 対応内容

- `/ai` の「抽出プレビューを作成」ボタンが入力空時に無効化され、押せないように見える挙動を改善
- ボタンは `loading` 時のみ無効化
- 入力空で押した場合は日本語エラー表示

## 変更ファイル

- `src/components/ai/ai-chat-panel.tsx`

## 実装詳細

1. `runExtract` 冒頭で空入力時に以下を表示
   - `課題文か授業メモを入力してください。`
2. ボタンの `disabled` 条件を `loading` のみに変更

## 検証

- `npm run check` PASS

