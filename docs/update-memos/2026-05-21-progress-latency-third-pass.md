# 2026-05-21 Progress Latency Third Pass

## 目的
- スマホ実機で進捗変更後の表示反映が遅い問題を追加調査。
- 課題詳細、課題一覧、作業チェックリストの進捗反映を最小変更で高速化。

## 変更内容
- 課題詳細の進捗ボタンを `pointerdown` 時点で即時反映するように調整。
- 作業チェックリストをクライアント側で楽観更新し、完了数と進捗パネルを即時更新。
- ステップ完了時の同期処理から Today 再生成を外し、チェック操作の待ち時間を削減。
- 課題一覧カードのステータス・締切表示をローカル状態で即時更新し、通常更新では `router.refresh()` を待たないように調整。
- 削除操作だけは一覧から消す必要があるため `router.refresh()` を維持。

## 確認
- `npm run lint`: PASS
- `npm run typecheck`: PASS
- `npm run check`: PASS
- `TASKFLOW_DEMO_MODE=1 npm run build -- --no-lint`: 長時間終了しなかったため中断扱い。5分ルールに従い、Vercel build確認へ切り替える。

## 残タスク
- Vercel build結果確認。
- 実機で課題詳細の進捗ボタン、作業チェックリスト完了、一覧カードのステータス変更を再確認。
